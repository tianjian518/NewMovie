# NewMovie · 云盘媒体库播放器 — 项目规划

> **一句话定义**：一个自托管的媒体服务器，直接对接 OpenList 把网盘当硬盘，自动刮削成海报墙，点开就能播，记得你看到哪儿。
>
> 代号 **NewMovie**（Video + Drive）。备选：PanFlix、Nebulix。名字最后你定。

---

## 一、产品定位

**一个容器，一个配置，从空白到海报墙。**

```
装容器 → 填 OpenList 地址 + token → 勾选要做成媒体库的目录 → 海报墙点击播放
```

对比现在社区的主流玩法（OpenList + strm 生成 + Emby 扫描 + MediaWarp 劫持 302 + Nginx 反代 = 5 个容器、5 套配置、5 个故障点），NewMovie 的核心差异是**把这五层压缩进一个容器**。

### 关于 strm 的立场：不强制生成，但完整支持消费

这是两件必须分开的事：

| | 立场 | 理由 |
| --- | --- | --- |
| **生成 strm** | 可选，非必需 | 我们自己就是媒体服务器，云端路径直接进数据库当一等公民即可，不需要先写一堆假文件再扫描自己 |
| **播放 strm** | **必须，一等公民** | 存量库不能让人重来；且 strm 是跨工具的事实标准 |

所以 NewMovie 支持**两种媒体库模式**，可以并存：

- **原生模式** — 直接对接 OpenList API / WebDAV，路径入库，无中间文件。**推荐，见第二节**
- **STRM 模式** — 指向已有的 strm 目录（OpenList strm 驱动落地的、AutoFilm/SmartStrm 生成的都行）
- **反向导出 strm**（Phase 3）— 给同时还想用 Kodi 的用户留条路

---

## 二、关于你的场景：OpenList strm 驱动这一层，其实可以省掉

你说主要用 OpenList 的 strm 驱动把网盘影视剧转成 strm。我把它的官方文档和实现机制查了一遍，**这里有个值得你重新考虑的点。**

### 2.1 OpenList strm 驱动到底做了什么

它是个**虚拟驱动**：读取你已挂载的路径（如 `/115_open/Video`），把里面的媒体文件对外**伪装成 `.strm` 文件**。而 strm 文件里写的内容是：

```
{Site URL}/d/115_open/Video/电影/再见，李可乐 (2023)/再见，李可乐 (2023) - 2160p.mkv
```

**注意这个 URL —— `/d/` 正是 OpenList 自己的 302 直链接口。**

所以整条链路实际是：

```
strm 文件  →  里面写着 OpenList 的 /d/ 链接  →  请求它  →  OpenList 302 到网盘真实直链
```

而 NewMovie 作为媒体服务器，完全可以跳过第一步：

```
直接调 OpenList API 列目录  →  拿到真实 .mkv  →  播放时请求 /d/ 或 raw_url  →  302 到网盘
```

**两条路的终点完全一样，但后者少了一整层。**

### 2.2 strm 驱动这层带来的实际代价

这些不是我推测的，是 OpenList 官方文档里白纸黑字写的限制：

| 官方文档原文 | 实际后果 |
| --- | --- |
| "生成本地文件功能**仅会在用户访问相应目录时生效**" | 这就是飞牛论坛里满屏 **"strm 文件夹一直是空的"** 的真正原因 |
| 递归生成需去 `管理界面/索引/手动扫描` | 每次都得手动跑一遍 |
| "**暂不支持定时或监听驱动更改自动生成**" | **网盘里新增了剧集，媒体库不会自动更新** |
| 需开启"写入操作后触发目录更新钩子" | 只覆盖上传/重命名/删除，且是额外配置项 |
| "Strm 采用本地代理方式…会消耗部署机器的流量" | 多一次无谓的流量与请求 |
| 只支持只读，不支持复制/删除/重命名 | 功能受限 |

再加上：本地落地的 strm 目录要占空间、要配"新增/更新/同步"三种模式、同步模式配错了还会删文件。

### 2.3 我的建议

**对 NewMovie 而言，strm 驱动是为 Emby 这类"只认本地文件"的服务器准备的适配层。而我们不需要这层适配。**

| | 原生模式（推荐） | STRM 模式 |
| --- | --- | --- |
| 配置 | 填 OpenList 地址 + token，勾目录 | 配 strm 驱动 + 落地路径 + 保存模式 + 手动扫描 |
| 新增剧集 | **增量扫描自动发现** | 需重新触发生成 + 重新扫描 |
| 磁盘占用 | 无 | 数万个小文件 |
| 额外流量 | 无 | strm 本地代理消耗 |
| 元数据 | API 直接给 size/mtime/thumb | strm 只有一行文本，需额外探测 |
| 出错概率 | 低 | 方言、路径、签名、同步模式，处处是坑 |

**所以：两种都做，但把原生模式作为推荐路径，strm 模式作为兼容路径。** 你现有的 strm 库能无缝导入继续用，同时可以随时切到原生模式——两种库还能并存对比，你自己感受哪个顺手。

### 2.4 既然主场景是 OpenList，就做原生 API 驱动，而不只是通用 WebDAV

这是我基于你的场景做的一个架构调整：**把「OpenList 原生 API 驱动」提升为 Phase 1 的首要驱动**，通用 WebDAV 作为兜底备选。

理由很实在：

| 能力 | OpenList 原生 API | 通用 WebDAV |
| --- | --- | --- |
| 列目录 | `/api/fs/list` 一次返回 JSON，含 size/mtime/thumb/is_dir | PROPFIND 解析 XML，大目录明显更慢 |
| 拿直链 | `/api/fs/get` 直接返回 **`raw_url`**（网盘真实直链，可绕过 OpenList 中转） | 只能走 WebDAV 流，或自行拼 `/d/` |
| 签名 | 拿到 token 后**自己算 sign** | 得让用户关闭签名 |
| 存储发现 | 能列出你挂了哪些网盘，UI 上直接勾选 | 只能盲填路径 |
| 刷新控制 | `refresh` 参数可控是否强制回源，**风控友好** | 无此粒度 |

> **签名这条我想单独说一下。** 现在社区教程几乎都在教人"全局设置里关闭签名"——这等于把你的网盘目录对任何知道地址的人敞开。NewMovie 拿到 token 后自己计算签名即可，**你完全不必为了能用而降低安全性**。

---

## 三、系统架构

```
┌──────────────────────────────────────────────────────────┐
│  Web UI  (React + TS + Vite + Tailwind)                  │
│  海报墙 │ 详情页 │ 播放器 │ 继续观看 │ 收藏 │ 搜索 │ 设置  │
└───────────────────────┬──────────────────────────────────┘
                        │  REST API + JWT
┌───────────────────────┴──────────────────────────────────┐
│  Backend (Go, 单二进制)                                   │
│                                                           │
│  ┌─ Storage Layer 存储抽象 ──────────────────────────┐   │
│  │   interface { List / Stat / Link / Read }          │   │
│  │   ├─ OpenList API ★ ← 主力：fs/list、raw_url、签名 │   │
│  │   ├─ WebDAV        ← 兜底：坚果云、群晖、其他       │   │
│  │   ├─ Local         ← 本地磁盘、NFS、SMB 挂载点     │   │
│  │   └─ 原生网盘驱动（Phase 4，自研）                  │   │
│  └────────────────────────────────────────────────────┘   │
│  ┌─ STRM Resolver 解析器 ──── 方言识别/路径重写      ┐   │
│  ┌─ Scanner 扫描器 ──── 增量 diff / 限速 / 断点续扫  ┐   │
│  ┌─ Parser 识别器  ──── 文件名 → 标题+年份+季集      ┐   │
│  ┌─ Scraper 刮削器 ──── NFO 优先 → TMDB，图片缓存    ┐   │
│  ┌─ Probe 探测器   ──── 编码/时长/音轨，懒加载       ┐   │
│  ┌─ Playback 播放服务 ── 五级播放策略（见第六节）     ┐   │
│  ┌─ User 用户系统  ──── 多用户/进度/收藏/权限        ┐   │
│  └─ Store ─────────────  SQLite（默认）/ PostgreSQL   ┘   │
└───────────────────────────────────────────────────────────┘
```

### 技术选型与理由

| 层 | 选型 | 为什么 |
| --- | --- | --- |
| 后端 | **Go** + Gin | 单二进制、无运行时依赖，Alpine 镜像可压到 ~40MB；协程天然适合大量并发 I/O（扫描、流转发）；交叉编译 arm64 一行命令，NAS 用户友好 |
| 数据库 | **SQLite**（默认，WAL 模式） | 零配置是产品体验的一部分。留 GORM 抽象层，重度用户可切 PostgreSQL |
| 前端 | **React + TS + Vite + Tailwind + shadcn/ui** | 海报墙是重交互场景，需要虚拟滚动 + 懒加载；shadcn 省掉大量 UI 打磨时间 |
| 播放器 | **ArtPlayer** + hls.js | 国内生态成熟，支持外挂字幕、倍速、快捷键、弹幕扩展；HLS 走 hls.js |
| 转码 | **FFmpeg**（可选镜像 tag） | 只在必要时启用，见第四节 |
| 元数据 | **TMDB** 主源 + TVDB 补充 | TMDB 中文数据全、免费、有官方 API |

---

## 四、STRM 支持（兼容你的存量库）

参考网易爆米花、飞牛影视的做法，但要解决它们没解决好的问题。

### 4.1 OpenList strm 驱动的三种输出形态（你的场景）

OpenList 的 strm 驱动有三个开关直接决定文件里写什么，NewMovie 必须三种都认：

| 开关组合 | strm 文件内容 |
| --- | --- |
| 默认（填了 Site URL） | `http://nas:5244/d/115_open/Video/电影/再见，李可乐 (2023).mkv` |
| 开启 **Encode Path** | `http://nas:5244/d/115_open/Video/%E7%94%B5%E5%BD%B1/...mkv` |
| 开启 **Without Url** | `/115_open/Video/电影/再见，李可乐 (2023).mkv` |
| 全局开启签名时 | 上述 URL 追加 `?sign=xxxx:0` |

> 补充一个坑：Site URL 留空时，OpenList 会**用你当时的访问地址做前缀**。如果你曾用 `localhost:5244` 访问过管理页，生成的 strm 里就会写死 `http://localhost:5244/...`——NewMovie 在容器里请求这个地址会指向它自己。这类问题靠 4.2 的重写规则一条搞定。

### 4.2 strm 没有统一规范 —— 这是最大的坑

`.strm` 就是个纯文本文件，里面写一行路径。但不同工具写出来的内容**方言完全不同**：

| 方言 | strm 内容示例 | 产出工具 |
| --- | --- | --- |
| **完整 HTTP 直链** | `http://192.168.1.10:5244/d/quark/电影/流浪地球.mkv` | **OpenList strm 驱动**、AutoFilm |
| **带签名直链** | `http://nas:5244/d/quark/xx.mkv?sign=abc123:0` | OpenList 开启签名时 |
| **URL 编码后** | `http://nas:5244/d/quark/%E7%94%B5%E5%BD%B1/xx.mkv` | OpenList 开 Encode Path |
| **纯内部路径** | `/quark/电影/流浪地球.mkv` | OpenList 开 Without Url、aliststrm |
| **服务中转链** | `http://nas:8095/strm/file?id=1234&sign=xxx` | SmartStrm（文件编号模式） |
| **本地绝对路径** | `/mnt/cd2/quark/电影/流浪地球.mkv` | CloudDrive2 挂载方案 |
| **相对路径** | `流浪地球.mkv` | Kodi 传统用法 |

**再叠加编码问题**：UTF-8 有无 BOM、URL 是否已编码、中文与特殊符号、Windows 反斜杠路径、CRLF 换行。

> 飞牛论坛里最高频的求助帖就是"strm 文件夹一直是空的""路径没设置对"——前者是 OpenList「访问才生成」的机制导致，后者本质就是方言不匹配却没有任何提示。

### 4.3 我们的做法：自动识别 + 显式重写规则

**解析流程：**

```
读取 .strm（自动去 BOM、trim、兼容 CRLF、跳过 # 注释行）
   ↓
方言嗅探：
   http(s)://  → 直链模式（自动识别 /d/ 形态 → 归一为 OpenList 存储路径）
   以 / 开头且能匹配已配置存储的挂载前缀 → 存储内部路径
   以 / 开头且存在于本地文件系统 → 本地路径
   其余 → 相对路径（相对 strm 文件所在目录）
   ↓
应用路径重写规则（用户可配，支持正则）
   例：^http://localhost:5244/d/(.*)$  →  openlist://main/$1
   ↓
解析为统一的 MediaSource 对象 → 交给播放降级链
```

**两个关键设计：**

**① 路径重写规则表** — 存量库平滑迁移的核心。strm 里写死了 `http://localhost:5244/...`（容器里根本访问不通）？配一条规则即可，不必重新生成几万个文件。爆米花和飞牛都没做好这块。

**② `/d/` 链接归一化 ★** — 这条是专为你的场景设计的：NewMovie 识别出 strm 里是 OpenList 的 `/d/` 链接后，**不会傻乎乎地直接去请求那个 URL**，而是把它还原成「OpenList 存储 + 内部路径」，然后当作原生资源处理。

好处很直接：

- 签名过期或配置变了 → 自己用 token 重算，不受 strm 里那个旧签名影响
- 可以调 `/api/fs/get` 拿 `raw_url`，**直连网盘 CDN，绕开 OpenList 中转**，起播更快
- strm 库和原生库共享同一套缓存、限速、直链管理

**换句话说：即使你继续用 strm 模式，NewMovie 也会在内部把它拉回原生模式的高速路上。**

### 4.4 strm 的元数据缺失问题

strm 文件本身只有一行文本，**没有时长、分辨率、编码、音轨信息**。这导致：

- 海报墙不显示时长
- 进度条不知道总长，"继续观看"位置算不准
- 不知道编码，无法提前决策该用哪级播放策略

**对策：三级探测策略（按代价递增，用户可配）**

| 模式 | 行为 | 适用 |
| --- | --- | --- |
| **懒探测**（默认） | 入库不探测，首次播放时探测一次并写库 | 平衡，推荐 |
| **后台探测** | 入库后放进低优先级队列，限速慢慢跑 | 大库且不急 |
| **不探测** | 完全依赖 TMDB 的时长数据 | 极度惧怕风控 |

探测手段优先级：`HTTP Range 取头部几百 KB 解析 moov/EBML` > `ffprobe -analyzeduration 限制` > 放弃。**绝不整文件下载探测**——那等于自杀式触发风控。

> 顺带一提：**原生模式下这个问题轻得多**，OpenList 的 `/api/fs/list` 直接就返回了 size 和 modified，只差编码信息。

### 4.5 直面飞牛的那个痛点：strm 里的 MKV 播不了

> 论坛原话："设置好后在飞牛影视挂载 strm 后可以正常播放 mp4 文件，但 mkv 文件就提示『不支持的视频格式』"

原因很简单：飞牛把 strm 里的 URL 直接扔给了浏览器 `<video>`，而浏览器**不认识 MKV 容器**。

NewMovie 的处理是：**strm 解析出来的资源，和原生挂载的资源走完全相同的五级降级链**（见第六节）。MKV 内封 H.264 → L2 Remux 秒播；H.265 不硬解 → L3 转码或 L4 唤起外部播放器。用户永远不会看到一句冷冰冰的"不支持的视频格式"。

**这是我认为 NewMovie 相对同类产品最实在的一个优势。**

### 4.6 strm 库的配套细节

- **同目录刮削文件复用**：`.nfo` / `poster.jpg` / `fanart.jpg` / `.srt` / `.ass` 优先读取，省去重复刮削（OpenList strm 驱动的"下载文件类型"参数、AutoFilm、SmartStrm 都会把这些同步下来）
- **死链检测**：后台低频校验（HEAD 请求），失效的标红并在 UI 上给出"重新定位"入口，而不是等用户点开才发现播不了
- **混合库**：同一个媒体库里 strm 和真实视频文件混放要能正常工作
- **一键切换到原生模式**：识别出这个 strm 库其实指向某个 OpenList 路径后，UI 上提示"可切换为原生模式"，**保留播放记录和收藏**，一键完成
- **反向导出**（Phase 3）：把 NewMovie 的库导出成 strm 目录，供 Kodi 等其他播放器使用

---

## 五、网盘场景的特殊性 — 这决定项目成败

普通媒体服务器面对的是本地硬盘：随机读、零延迟、无限次访问。网盘完全不同，三个必须正面解决的问题：

### 1. 风控（最致命）
115、夸克、天翼对高频 API 调用极其敏感，扫描 5000 个文件可能直接触发限流封禁数小时。

**对策：**
- **令牌桶限速**：全局 QPS 可配（默认保守值 2 req/s），按存储源独立计数
- **目录树缓存**：SQLite 持久化，TTL 可配，扫描优先读缓存
- **增量扫描**：对比 `path + size + mtime` 三元组，未变更目录整棵跳过
- **断点续扫**：扫描任务持久化，中断后从断点继续，不做无意义的全量重来
- **退避重试**：遇 429/403 指数退避，而不是硬刚
- **复用 OpenList 自身的缓存 ★**：调 `/api/fs/list` 时默认 `refresh=false`，让 OpenList 用它自己的目录缓存回答，**根本不碰网盘 API**。只在用户手动点"强制刷新"时才 `refresh=true`。这一条几乎把常规扫描的风控风险降到零——**接原生 API 而不是 WebDAV 的又一个理由**

### 2. 直链的时效与防盗链
网盘直链通常 15 分钟到 8 小时过期，且常校验 UA / Referer / IP。

**对策：** 直链缓存表带 `expires_at`，播放前检查剩余有效期（<5 分钟则重新获取）；防盗链场景自动降级到服务端代理转发（见下节 Level 1）。

### 3. Seek 依赖 Range 请求
拖进度条本质是 HTTP Range 请求。探测存储源是否支持 Range，不支持的源在 UI 上明确标注"不可拖动"，而不是让用户点了没反应干瞪眼。

---

## 六、播放策略：五级降级链（核心设计）

> **无论资源来自原生挂载还是 strm 解析，都走这同一条链。**

网盘里的资源大多是 `MKV + H.265 + DTS/TrueHD`，而浏览器 **不认识 MKV 容器**，HEVC 还依赖 GPU 硬解。硬上转码则会让树莓派、低配 NAS 直接跪掉。

所以播放不是一个开关，而是一条**自动降级链**——按代价从低到高逐级尝试：

| 级别 | 策略 | 触发条件 | 服务端开销 |
| --- | --- | --- | --- |
| **L0** | **302 直链** | MP4/WebM 容器 + H.264/AAC | **零**（流量不过服务器） |
| **L1** | **代理转发** | 有防盗链，需补 UA/Referer | 仅带宽，无 CPU |
| **L2** | **Remux 封装** | MKV 容器，但编码浏览器支持 | 极低（`-c copy` 不重编码） |
| **L3** | **真转码** | H.265 不硬解 / DTS/TrueHD 音轨 | 高（需 ffmpeg 镜像） |
| **L4** | **外部播放器** | 上述都不合适，或用户偏好 | 零 |

**L0 在 OpenList 场景下有两种直链，优先级不同：**

| 来源 | 说明 | 何时用 |
| --- | --- | --- |
| `raw_url`（`/api/fs/get` 返回） | **网盘 CDN 真实直链**，完全不经过 OpenList | 首选，起播最快、OpenList 零负载 |
| `/d/{path}?sign=` | 走 OpenList 302 中转 | raw_url 拿不到、或有防盗链需 OpenList 补 header 时 |

**几个我认为重要的判断：**

- **L2 是性价比之王。** 大量 MKV 其实内含 H.264/AAC，只是容器不对。`ffmpeg -c copy` 重封装成 fMP4/HLS 几乎不吃 CPU，却能把"播不了"变成"秒播"。这一级必须做好。
- **L3 默认关闭。** 转码是资源黑洞，做成可选镜像 tag（`:full` 含 ffmpeg，`:slim` 不含），让用户自己选。
- **L4 不是失败兜底，是一等公民。** 追求原盘画质音质的用户本来就该用 PotPlayer / IINA / Infuse / VLC。一键唤起 + 直链传递，并且**播放进度能回传**——这是很多同类方案做得很糟的地方。

**降级决策靠预探测**：扫描入库时用 `ffprobe`（或轻量 MP4/MKV box 解析）读一次编码信息存库，播放时直接查表决策，不临时探测拖慢起播。strm 库因风控顾虑默认改用懒探测（见 4.4）。

**降级过程对用户可见但不打扰**：播放器角落用小标签标明当前策略（`直链` / `代理` / `重封装` / `转码`），出问题时用户和我们都能快速定位，而不是只能看到一个转圈。

---

## 七、元数据识别与刮削

这是决定"海报墙好不好看"的关键，也是最容易翻车的地方——中文资源命名极其混乱。

```
识别流程：
文件路径 → 清洗（去 [组名]/1080p/WEB-DL/H265/国语中字…）
        → 提取 标题 / 年份 / 季 / 集
        → TMDB 搜索（中文优先，失败回退英文/原名）
        → 置信度评分 → 高分自动入库 / 低分进"待确认"队列
```

- **剧集识别**：覆盖 `S01E01` / `第01集` / `EP01` / `[01]` / 纯数字等常见形态
- **NFO 优先**：目录里已有 `.nfo`（AutoFilm/SmartStrm/tinyMediaManager/Emby 常已生成）则直接读取，跳过 TMDB 请求——既快又省 API 配额
- **强制解决歧义**：支持在目录放 `.newmovie.json` 手动锁定 TMDB ID，一劳永逸
- **人工纠错 UI**：识别错了能一键搜索改绑，不能只能干瞪眼
- **图片本地化**：优先用同目录 `poster.jpg`/`fanart.jpg`，否则从 TMDB 下载；统一转 WebP 缩略图，海报墙滚动才不卡
- **字幕**：同目录 `.srt/.ass` 自动关联；ASS 特效字幕前端用 SubtitlesOctopus 渲染；内封字幕按需提取

### 7.1 已实现：刮削器（`internal/scraper` + `internal/tmdb`）

> 状态：**已落地**（2026-08-01）。NFO 优先、同目录本地图最高优先级、TMDB 兜底。零外部依赖（TMDB 走 `net/http`）。

刮削在扫描器 `ingest` 之后触发。对每个条目，扫描器先用「同目录文件名清单」推导候选路径（无需额外列目录请求），再按以下**优先级合并**：

| 维度 | 优先级（高 → 低） | 说明 |
| --- | --- | --- |
| **图片** | 同目录本地图 > NFO 远程 `thumb`/`fanart` > **TMDB** | 用户自己放的图永远最准 |
| **tmdb_id / 标题 / 年份 / 简介** | **NFO** `uniqueid[type=tmdb]` > **TMDB**（有 id 走 `ByID`，否则 `Search`）> 解析器结果 | NFO 给出的 id 直接 `ByID` 拉详情，最准 |

- **NFO 解析**：兼容 `movie` / `tvshow` / `episodedetails` 根元素（`encoding/xml`，纯函数、可单测）。提取 `uniqueid[type=tmdb]`、`title`、`year`/`premiered`、`plot`/`outline`、`thumb`（海报）、`fanart>thumb`（背景）。
- **同目录本地图不存直链**：`poster.jpg`/`fanart.jpg` 的网盘直链会过期，故 `MediaItem.PosterURL/BackdropURL` 只存**服务端代理路径**（`/api/items/:id/poster`、`/api/items/:id/backdrop`），由服务端经 OpenList 取直链后再流式转发（支持 `Range`）。
- **TMDB 客户端**（`internal/tmdb`）：`Search`（电影/剧集/多端）、`ByID`、`ImageURL`。`HTTP` 客户端可注入，单测用 `httptest` 桩，无外网也能测。中文结果 `language=zh-CN`。`TMDB_API_KEY` 环境变量注入；**为空则完全跳过 TMDB**，仅靠 NFO + 解析器。
- **重刮削**：详情页「重新刮削」按钮 → `POST /api/items/:id/rescrape`，保留已存本地图路径，按 TMDB 兜底重跑（适合初次扫描未配 Key、补配后再跑）。
- **增量缓存（避免重复刮削）**：扫描器对已刮削过的条目（已有 `poster_url`）直接跳过，不再读 NFO / 不再打 TMDB；只有首次扫描、或「重新刮削」、或之前未刮成功的条目才会真正请求。元数据本身持久化在 `newmovie.json`，**打开海报墙只读库、不刮削**。
- **用户自填 Key（持久化）**：TMDB Key 既可用环境变量 `TMDB_API_KEY`，也可在「设置」页填入并持久化（`POST /api/settings`，存于本机 `newmovie.json`）。环境变量优先，其次取设置。Key 仅自己用，不内置到镜像。
- **已知边界（MVP）**：剧集的 `tvshow.nfo` 通常在剧集根目录而非分季目录，当前按**当前目录**找 `tvshow.nfo`，找不到时回退 TMDB 按剧名搜索；后续可向上一级目录递归查找。本地图代理目前每次实时经 OpenList 取直链（已带 ETag/Last-Modified 供浏览器缓存），后续可加服务端内存缓存进一步减负。

### 7.2 关于你的 139cas 魔改版 OpenList

`https://github.com/tianjian518/139cas` 已确认是 OpenList 的 rebrand（加了广哑盘驱动 + `.cas` 真实文件预览）。**签名算法继承自原版 OpenList**，NewMovie 的 `openlist.Sign()` 与其一致，无需改动即可对接。`.cas` 文件（`/d/*.cas`、`/p/*.cas`）的 CAS 预览能力作为 **Phase 4** 的 fork 专属模式后续接入。

---

## 八、功能清单与阶段划分

### Phase 1 — MVP（能用）
- [x] 存储抽象层 + **OpenList 原生 API 驱动（含 token 鉴权、自动算 sign、raw_url）** + 本地驱动
- [x] **OpenList 存储发现**：连上后列出你挂了哪些网盘，勾选目录即建库
- [x] **STRM 解析器：方言自动识别 + `/d/` 链接归一化 + 路径重写规则 + 懒探测**
- [x] 媒体库配置（原生模式 / STRM 模式 / 混合，多库）
- [x] 扫描器：全量 + 增量 + 限速（默认 `refresh=false` 复用 OpenList 缓存）+ 进度展示 + **strm/mixed 模式**
- [x] 文件名识别 + **NFO 优先 + 同目录本地图代理 + TMDB 兜底** + 详情页重刮削
- [x] 海报墙 UI（虚拟滚动、懒加载）+ 详情页
- [x] 播放 L0（raw_url 优先）/L1 + ArtPlayer + 外挂字幕
- [x] 播放进度记录 + 继续观看
- [ ] 单用户登录、Docker 镜像上架 Docker Hub

> 通用 WebDAV 驱动挪到 Phase 2 —— 你的主场景是 OpenList，原生 API 在**性能、风控、签名**三方面都明显更优，先把它做扎实。

### Phase 2 — 好用
- [ ] **通用 WebDAV 驱动**（坚果云、群晖等非 OpenList 场景）
- [ ] 多用户 + 权限（媒体库级隔离）+ 儿童模式
- [ ] 收藏 / 想看 / 已看标记 / 评分
- [ ] 剧集追踪：下一集、季进度、自动连播
- [ ] 全局搜索（标题/演员/类型）+ 筛选排序
- [ ] L4 外部播放器唤起 + 进度回传
- [ ] **strm 死链检测 + 批量重新定位 + 一键转原生模式**
- [ ] 演职员页、类型/年代聚合页

### Phase 3 — 强大
- [ ] L2 Remux + L3 转码（`:full` 镜像）— **strm 里的 MKV 从此不再"不支持"**
- [ ] 内封字幕/多音轨切换
- [ ] **反向导出 strm**（供 Kodi 等外部播放器复用媒体库）
- [ ] 移动端适配 + PWA
- [ ] 观看统计、随机推荐
- [ ] Webhook / 通知（Telegram、Bark）

### Phase 4 — 生态
- [ ] 自研原生网盘驱动（摆脱对 OpenList 的依赖）
- [ ] 弹幕（对接弹幕库）
- [ ] 多端客户端探索

---

## 九、Docker 交付方案

```dockerfile
# 多阶段构建
Stage 1: node:22-alpine   → 构建前端静态资源
Stage 2: golang:1.23-alpine → 编译后端（静态资源 embed 进二进制）
Stage 3: alpine:3.20      → 最终镜像
```

| 镜像 Tag | 内容 | 体积（预估） |
| --- | --- | --- |
| `newmovie:slim` / `:latest` | 纯后端，L0/L1/L2(有限) | ~40 MB |
| `:full` | 含 FFmpeg，支持 L2/L3 | ~180 MB |

- **多架构**：`linux/amd64` + `linux/arm64`（覆盖 x86 NAS 与 ARM 设备）
- **CI/CD**：GitHub Actions，打 tag 自动构建并推送 Docker Hub，语义化版本
- **数据持久化**：单挂载点 `/data`（含 SQLite、图片缓存、配置、日志）

```yaml
services:
  vidrive:
    image: tianjian518/newmovie:latest
    ports: ["8096:8096"]
    volumes:
      - ./data:/data
      # 若要用 STRM 模式，把 OpenList 落地的 strm 目录挂进来（只读）
      # - /volume1/docker/openlist/strm:/media/strm:ro
    environment:
      - TZ=Asia/Shanghai
    restart: unless-stopped
```

> **网络提示**：若 NewMovie 与 OpenList 在同一 Docker 网络，可直接用容器名 `http://openlist:5244` 互访；跨宿主机则填局域网 IP。**不要填 `localhost`**——那指向容器自身。

---

## 十、数据模型（核心表）

| 表 | 用途 |
| --- | --- |
| `storages` | 存储源配置（类型、地址、**token 加密存储**、签名密钥、限速参数） |
| `libraries` | 媒体库（名称、类型、**模式：原生/STRM/混合**、绑定路径、刮削设置） |
| `media_items` | 影片/剧集主体（TMDB ID、标题、简介、评分、海报…） |
| `seasons` / `episodes` | 剧集层级 |
| `media_files` | 物理文件（路径、大小、编码信息、Range 支持、**来源类型 native/strm**、**strm 原始内容**、探测状态） |
| `path_rewrites` | **strm 路径重写规则（正则 → 目标存储/路径，带优先级）** |
| `link_cache` | 直链缓存（raw_url / `/d/` URL、过期时间、所需 headers） |
| `users` | 用户与权限 |
| `play_records` | 播放进度（用户 × 文件 × 位置 × 时间） |
| `favorites` | 收藏 / 想看 |
| `scan_jobs` | 扫描任务状态与断点 |

> `media_files` 保留 strm 原始内容是有意为之：解析规则改了、重写规则调了，可以整库重算而不必回头重读几万个文件。

---

## 十一、主要风险与应对

| 风险 | 影响 | 应对 |
| --- | --- | --- |
| 网盘风控封禁 | **高** — 直接不可用 | 保守限速、增量扫描、指数退避、**默认 `refresh=false` 复用 OpenList 缓存** |
| strm 方言不匹配 | **高** — 整库播不了 | 自动嗅探 + `/d/` 归一化 + 重写规则 + **配置时实时预览解析结果** |
| OpenList 地址写死 localhost | 中 — 容器内不通 | 配置时连通性检测 + 明确报错提示 + 重写规则修正 |
| 中文命名识别率低 | 中 — 海报墙一堆"未识别" | NFO 优先 + 多规则库 + 置信度 + 手动纠错 UI |
| 浏览器编码兼容 | 中 — 点了播不了 | 五级降级链 + 探测 + 外部播放器兜底 |
| strm 元数据缺失 | 中 — 无时长、进度不准 | 三级探测策略 + TMDB 时长兜底（原生模式无此问题） |
| OpenList API 变更 | 中 — 驱动失效 | 驱动层隔离，WebDAV 作为兜底通路 |
| 首次扫描耗时长 | 中 — 劝退新用户 | 边扫边入库、实时进度条、先出海报墙再补细节 |
| 大库性能 | 中 — 万级条目卡顿 | 虚拟滚动、索引优化、缩略图 WebP、分页 API |

> **"配置时实时预览"这一条我想特别强调。** 飞牛论坛里满屏的"strm 文件夹一直是空的""路径没设置对"，根源都是配置阶段零反馈，用户只能靠猜。配置页面上放一个"测试解析"按钮，当场把结果和连通性显示出来，能消灭掉这一整类求助帖。

---

## 十二、需要你拍板的几件事

1. **项目名**：NewMovie / PanFlix / Nebulix / 你来定
2. **要不要接受"原生模式优先"的建议**（见第二节）：我的方案是两种都做，但推荐你逐步切到原生模式，省掉 strm 生成、手动扫描、无法自动更新这一堆麻烦。**strm 模式仍会完整支持，你的存量库能直接用。**
3. **你的 OpenList 是怎么部署的**：和 NewMovie 同一台机器 / 同一 Docker 网络 / 不同机器？地址端口是什么？**有没有开签名？**
4. **给我看一个你现有的 strm 文件内容**（就一行文本）：我按你的实际方言优先适配，别的往后排
5. **转码要不要做**：建议 MVP 先不做，Phase 3 补 `:full` 镜像
6. **Docker Hub 账号**：用户名 + 后续推送的凭据配置方式
7. **UI 风格倾向**：Emby 深色紧凑型，还是 Netflix 大图横向滑动型

---

## 十三、默认决策（在你拍板前，我先按这些假设开工）

你还没回这 7 个问题，但我先把项目骨架搭起来——以下都是**可一键推翻**的默认，看到不顺手的告诉我即可。

| # | 问题 | 我的默认 | 理由 |
| --- | --- | --- | --- |
| 1 | 项目名 | **NewMovie** | 沿用规划代号 |
| 2 | 原生模式优先 | **接受**，两种都做、原生默认 | 已在第二节论证：省 strm 生成/手动扫描/无法自动更新 |
| 3 | OpenList 部署 | **同一 Docker 网络，容器名 `http://openlist:5244`，签名开启** | 同网络最稳；签名开 → NewMovie 自行算 sign，安全性不降级（见 2.4） |
| 4 | strm 样本 | **不依赖单一样本**，全方言通用解析 | 解析器对 7 种方言 + 编码坑都做，无需你贴样本也能跑 |
| 5 | 转码 | **MVP 不做，Phase 3 补 `:full` 镜像** | 转码是资源黑洞，先靠 L0/L1/L2/L4 |
| 6 | Docker Hub | 镜像名占位 `tianjian518/newmovie`，CI 已写好 | 推送需你的账号+令牌，给到后填 `.github/workflows/docker.yml` 的 secrets 即可 |
| 7 | UI 风格 | **Emby 深色紧凑 + 海报墙大图**（混合） | 信息密度与海报观感兼顾 |

### 一处主动偏离原规划（已验证为更优）

原规划后端用 **Gin**。因当前构建环境**无外网出口**（`proxy.golang.org` / npm / GitHub 均不可达），我改用 **Go 标准库 `net/http`** 实现后端，做到**零外部依赖、单二进制、本沙箱即可编译运行与测试**。这反而更贴合"单二进制 ~40MB"目标，API 形态完全不变，将来想换 Gin 也只是 handler 层替换。SQLite 同理：用 JSON 文件持久化（抽象在 `Store` 接口后），生产环境替换为 `modernc.org/sqlite`（纯 Go，无 CGO）是十几行的事。

> 前端与 Docker 构建仍需要网络（React/Vite、拉基础镜像），故在本环境给出**完整源码 + 配置**，你在有网机器上 `npm install` / `docker build` 即出成品。
